import Foundation

/// Metadata for the Postgres integration target.
public enum KHKPostgres {
    /// Semantic version of the Postgres target (tracks the main package).
    public static let version = "0.1.0"
}
